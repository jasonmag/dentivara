<% content_for :title, "Edit Appointment" %>

<div class="space-y-4">
  <div class="page-header">
    <h1 class="page-title">Edit Appointment</h1>
    <%= link_to "Back to appointments", appointments_path, class: "btn btn-secondary" %>
  </div>

  <div class="app-card">
    <%= render "form", appointment: @appointment %>
  </div>

  <%= link_to "Show appointment", @appointment, class: "btn btn-secondary" %>
</div>
