<% content_for :title, "Editing appointment" %>

<div class="page-header">
  <h1 class="page-title">Editing appointment</h1>
  <%= link_to "Back to appointments", appointments_path, class: "btn btn-secondary" %>
</div>

<div class="app-card">
  <%= render "form", appointment: @appointment %>

  <div class="mt-4">
    <%= link_to "Show this appointment", @appointment, class: "btn btn-secondary" %>
  </div>
</div>
