<% content_for :title, "Users" %>

<div class="space-y-8">
  <section class="flex items-end justify-between">
    <div>
      <h1 class="page-title">Users</h1>
      <p class="serene-subheading mt-1">Manage clinic team roles and access.</p>
    </div>
    <%= link_to "New User", new_user_path, class: "btn btn-primary" %>
  </section>

  <section class="table-shell">
    <table class="data-table">
      <thead>
        <tr>
          <th>Name</th>
          <th>Email</th>
          <th>Role</th>
          <th>Updated</th>
          <th class="text-right">Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @users.each do |user| %>
          <tr>
            <td class="font-semibold text-stone-800"><%= user.name %></td>
            <td><%= user.email %></td>
            <td><%= user.role.humanize %></td>
            <td><%= l(user.updated_at, format: :short) %></td>
            <td class="text-right"><%= link_to "View", user_path(user), class: "btn btn-secondary px-3 py-1.5 text-xs" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </section>
</div>
